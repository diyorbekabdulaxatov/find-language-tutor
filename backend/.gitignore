# Binaries (`go build ./cmd/<name>` drops the binary in the cwd)
/bin/
/api
/worker
/migrate
/seed
*.exe

# Local env
.env

# Local blob store (uploaded resource files in dev)
/.localdata/

# Test / coverage
*.out
coverage.*

# OS
.DS_Store
