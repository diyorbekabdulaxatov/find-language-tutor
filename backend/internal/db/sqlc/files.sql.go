// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: files.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const createFileAsset = `-- name: CreateFileAsset :one

INSERT INTO file_assets (owner_id, provider, object_key, filename, content_type, bytes)
VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id, owner_id, provider, object_key, filename, content_type, bytes, created_at
`

type CreateFileAssetParams struct {
	OwnerID     uuid.UUID
	Provider    string
	ObjectKey   string
	Filename    string
	ContentType string
	Bytes       int64
}

// File assets (phase A1): the app's handle to a blob in the store.
func (q *Queries) CreateFileAsset(ctx context.Context, arg CreateFileAssetParams) (FileAsset, error) {
	row := q.db.QueryRow(ctx, createFileAsset,
		arg.OwnerID,
		arg.Provider,
		arg.ObjectKey,
		arg.Filename,
		arg.ContentType,
		arg.Bytes,
	)
	var i FileAsset
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.Provider,
		&i.ObjectKey,
		&i.Filename,
		&i.ContentType,
		&i.Bytes,
		&i.CreatedAt,
	)
	return i, err
}

const deleteAllFileAssets = `-- name: DeleteAllFileAssets :exec
DELETE FROM file_assets
`

// Seed-only. file_assets.owner_id -> users cascades, but resources may still
// reference an asset id in JSONB (no FK), so the seed clears this before users.
func (q *Queries) DeleteAllFileAssets(ctx context.Context) error {
	_, err := q.db.Exec(ctx, deleteAllFileAssets)
	return err
}

const getFileAsset = `-- name: GetFileAsset :one
SELECT id, owner_id, provider, object_key, filename, content_type, bytes, created_at
FROM file_assets
WHERE id = $1
`

func (q *Queries) GetFileAsset(ctx context.Context, id uuid.UUID) (FileAsset, error) {
	row := q.db.QueryRow(ctx, getFileAsset, id)
	var i FileAsset
	err := row.Scan(
		&i.ID,
		&i.OwnerID,
		&i.Provider,
		&i.ObjectKey,
		&i.Filename,
		&i.ContentType,
		&i.Bytes,
		&i.CreatedAt,
	)
	return i, err
}
